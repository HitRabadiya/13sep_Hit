#include <stdio.h>
int main() 
{
    int a, b, ans;
 
    // Input
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    // Addition
    ans = a + b;

    // Output
    printf("Ans = %d\n", ans);

    return 0;
}